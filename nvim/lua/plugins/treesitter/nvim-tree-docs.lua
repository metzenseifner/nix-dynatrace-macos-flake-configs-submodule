-- nvim-tree-docs is deprecated and uses removed treesitter API
-- It uses require("nvim-treesitter").define_modules which no longer exists
-- Disable until updated or replace with alternative
return {}
