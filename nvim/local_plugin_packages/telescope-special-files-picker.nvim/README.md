# telescope-special-files.nvim

A Telescope extension that adds a picker for special files like notes, standup
reports, one-on-ones, and anything else that does not fit into your development
projects.

# Installation

```
{ "nvim-telescope/telescope.nvim", dependencies = "metzenseifner/telescope-special-files.nvim" }
```
